<!-- 这是自定义组件的内部WXML结构 -->
<view class="loading">
    <view class="loading-box">
        <view wx:if="{{haveIcon}}" class="loading-icon"></view>
        <view>{{loadingText}}</view>
    </view>
</view>
